{
  "scripts": {
    "start": "cd frontend && ng serve --host=0.0.0.0 --port=4200",
    "test": "start-server-and-test start http-get://localhost:4200 test:run",
    "test:run": "cypress run --reporter mochawesome --reporter-options reportDir=mochawesome-report,overwrite=false,html=true,json=true"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.8.0",
    "cypress": "^14.4.0",
    "mochawesome": "^7.1.3",
    "start-server-and-test": "^2.0.0"
  }
}